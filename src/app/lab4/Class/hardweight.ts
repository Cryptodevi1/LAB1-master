import { Car } from "./car";

export class Hardweight extends Car
{
    constructor(override name: string, public max_weight: number)
    {
        super();
    }


S()

{
    let result;
    this.result = this.max_weight*100;
}
}